/**
 * Placeholder root for /vault/*. Scaffolded per SDD-01 §1.2 / PRD §3.1 —
 * app status: future. No route is registered for it yet in router.tsx.
 */
export function VaultHome() {
  return <p>Vault — planned.</p>;
}
