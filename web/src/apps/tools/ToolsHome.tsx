/**
 * Placeholder root for /tools/*. Scaffolded per SDD-01 §1.2 / PRD §3.1 —
 * app status: future. No route is registered for it yet in router.tsx.
 */
export function ToolsHome() {
  return <p>Tools — planned.</p>;
}
