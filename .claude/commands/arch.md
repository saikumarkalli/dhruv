Use the `dhruv-arch-guardian` agent for architecture questions, boundary checks, or to propose an ADR.

Arguments: $ARGUMENTS (describe the question, proposed change, or ADR topic)

Hand off to dhruv-arch-guardian with this task: $ARGUMENTS. Ground on platform/PLATFORM.md and platform/DECISIONS.md first. If a decision is already accepted, do not silently diverge — propose a new ADR under platform/adr/ instead. Module boundary rules are enforced by ArchUnit; any change that would require relaxing those rules needs an ADR. Write ADRs only — never edit source code.
