Use the `dhruv-debugger` agent to diagnose and fix a failing build, test, or crash.

Arguments: $ARGUMENTS (paste the error, stack trace, or describe the failure)

Hand off to dhruv-debugger with this task: diagnose and fix — $ARGUMENTS. Identify the root cause with minimal reproduction. Apply the smallest change that makes it green. Prove the fix by re-running the failing command. Do not refactor surrounding code beyond what is needed to fix the issue.
