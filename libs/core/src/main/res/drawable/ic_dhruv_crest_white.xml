<?xml version="1.0" encoding="utf-8"?>
<!-- White silhouette of the direction-1a flat mark — see ic_dhruv_crest.xml for the shape
     rationale. Used by DhruvNotificationIcon (Android requires a pure white-on-transparent
     silhouette for the status-bar/notification small icon; the OS applies its own tint) and by
     the Android 12+ SplashScreen API icon (Theme.DhruvSplash, values/themes.xml), which shows on
     black before the Compose splash takes over. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <path
        android:fillColor="#FFFFFF"
        android:pathData="M54,12 L64,44 L96,54 L64,64 L54,96 L44,64 L12,54 L44,44 Z" />
</vector>
